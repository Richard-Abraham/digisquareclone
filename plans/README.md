# Implementation Plans

Generated by the improve skill on 2026-06-25. Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

All plans planned at commit `cd4c6f5`. Before starting any plan, run its
drift check: `git diff --stat cd4c6f5..HEAD -- <in-scope paths>` — if any
in-scope file has changed, compare the plan's "Current state" excerpts
against the live code and report mismatches as a STOP condition.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Remove empty experimental key and unused CSS class | P3 | S | LOW | — | DONE |
| 002 | Deduplicate PRIO_META constant into lib/tasks.ts | P2 | S | LOW | — | DONE |
| 003 | Simplify pagination in dashboard page | P3 | S | LOW | — | TODO |
| 004 | Extract shared timer and dependency hooks | P2 | M | MED | — | TODO |
| 005 | Remove unused @tanstack/react-query dependency | P1 | S | LOW | — | TODO |

## Execution notes

- **Order**: 001 → 002 → 003 → 004 → 005 is the recommended order.
  Each leaves the build green independently, so they can also be parallelized
  (different branches/PRs) as long as imports are reconciled.
- **Merge conflicts**: Plans 002 and 003 both modify `dashboard/page.tsx`.
  If applied sequentially on the same branch, apply 002 first (adds import),
  then 003 (replaces pagination block). Plan 004 also touches
  `issues/[id]/page.tsx` which 002 touches — same ordering advice.
- **Testing**: `npm run build` (Next.js build = typecheck) is the primary
  gate. `npm test` runs vitest on `src/lib/tasks.test.ts` as a smoke test.
  There is no separate `npm run lint` in this project.

## Findings considered and rejected

- **TasksIcon duplication** (`icons.tsx` vs `dashboard/page.tsx`): the two
  copies use different dimensions (18×18 vs 24×24) and stroke widths (1.8
  vs 1.5) for different visual contexts. Unifying would add a `size` prop
  but the savings (~9 lines) don't justify the touch. Rejected per user choice.
- **pnpm switch**: considered and rejected per user choice (stick with npm).
- **Response envelope refactor** (`ok()`/`err()` coupling): the graph shows
  these have 106 edges each across 41 route files + 4 client files — working
  as designed and changing it would be high-cost for no concrete benefit.
- **`getAdmin()` per-request decoupling**: 127 edges, 9 communities —
  the coupling is architectural and changing it would touch 45+ files.
  Not worth it at this project scale.

## Status values

TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)
